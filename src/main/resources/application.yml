
management:
  endpoints:
    web:
      exposure:
        include: '*'
  info:
    env:
      enabled: true

info:
  application:
    name: AssignmentSubmissionApplication
    description: A blog with articles about The Spring Framework.
    author: Dan Vega
    version: '@project.version@'
spring:
  profiles:
    active: dev
